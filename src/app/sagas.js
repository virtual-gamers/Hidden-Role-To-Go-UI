// import {
//   call,
//   put,
//   takeLatest,
// } from 'redux-saga/effects';

// import actions

// generator function
function* fetchService(action) {
  // try {
  //   response = yield call(service?, params);
  //   if (!response || response.error_code) {
  //     yield put ({ type: FAILED_ACTION });
  //   } else {
  //     yield put ({ type: SUCCESS_ACTION });
  //   }
  // } catch(e) {
  //   yield put({ type: FAILED_ACTION });
  // }
}

function* fetchServiceSaga() {
  // yield takeLatest(ACTION, fetchService); 
}

export default [fetchServiceSaga];